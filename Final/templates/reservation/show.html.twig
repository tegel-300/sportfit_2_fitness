{% extends 'base.html.twig' %}

{% block title %}ReservationEvent{% endblock %}

{% block body %}
    <h1>ReservationEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_event.id }}</td>
            </tr>
            <tr>
                <th>Idevent</th>
                <td>{{ reservation_event.idevent }}</td>
            </tr>
            <tr>
                <th>Idclient</th>
                <td>{{ reservation_event.idclient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_index') }}">back to list</a>

    <a href="{{ path('app_reservation_edit', {'id': reservation_event.id}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
